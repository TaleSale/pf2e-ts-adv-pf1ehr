# Кнопка отправки в чат для @Rebellion enricher

## Описание

Добавлена новая функциональность для enricher'а `@Rebellion`, которая позволяет отправлять запросы на броски в чат, аналогично стандартным enricher'ам PF2e (например, `@Check`).

## Внешний вид

Теперь каждая кнопка `@Rebellion` дополнена маленькой иконкой чата:
- **Основная кнопка** - кнопка броска (работает как раньше, выполняет бросок немедленно)
- **Иконка чата** (💬) - маленькая кнопка рядом для отправки в чат

## Как использовать

### Вариант 1: Немедленный бросок (как раньше)
Нажмите на основную (левую) кнопку - бросок выполнится сразу.

### Вариант 2: Отправка в чат (новая функция)
1. Нажмите на иконку чата (💬) рядом с основной кнопкой
2. В чат отправится простая кнопка с названием проверки (например, "Верность")
3. Любой игрок может нажать эту кнопку для выполнения броска
4. Кнопка в чате работает точно так же, как основная кнопка - вызывает диалог и поддерживает реролл по ПКМ

## Примеры использования

```
@Rebellion[type:loyalty|dc:15]
@Rebellion[type:security|dc:20]
@Rebellion[type:secrecy|dc:25]
@Rebellion[type:notoriety]
@Rebellion[type:dangers]
```

## Преимущества

1. **Прозрачность** - все видят, какой бросок требуется
2. **Координация** - игроки могут обсудить бросок перед его выполнением
3. **История** - запросы на броски сохраняются в чате
4. **Гибкость** - можно выбрать между немедленным броском и отправкой в чат
5. **Полная функциональность** - кнопки в чате работают как обычные PF2e броски с диалогами и рероллами

## Технические детали

### Изменения в коде:
- `scripts/main.js` - добавлена кнопка чата и обработчики
- `styles/rebellion-sheet.css` - добавлены стили для новых кнопок

### Стиль кнопок:
- Основная кнопка: градиент от #4a5568 до #2d3748, обычные скругленные углы
- Иконка чата: маленькая серая кнопка 20x20px с иконкой комментария
- Кнопки расположены рядом с небольшим отступом

### Обработка бросков:
- d100 броски (известность, опасность) - показывают диалог с модификатором, используют стандартный Roll
- d20 броски (верность, безопасность, секретность) - используют PF2e Check API с полным диалогом
- Все броски поддерживают реролл по ПКМ как обычные PF2e броски
- После броска кнопка в чате отключается и становится серой
- Основная и чат кнопки используют одинаковую логику
